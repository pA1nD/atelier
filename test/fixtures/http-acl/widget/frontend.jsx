// `hidden` marks this as infrastructure — excluded from the rail / defaultUser,
// and (per the shell) exempt from the presence gate like a chrome.
export const meta = { hidden: true }
export default function Module() { return null }
